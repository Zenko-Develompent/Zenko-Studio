# Массивы и строки в Java

Строка — это последовательность символов. У строки можно узнать длину методом `length()`.

```java
String s = "HELLO";
System.out.println(s.length());
```

Массив хранит несколько значений одного типа, например `int[]`.
