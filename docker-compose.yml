version: '3.8'

services:
  db:
    image: postgres:18
    container_name: edu-db
    environment:
      POSTGRES_DB: edu_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"